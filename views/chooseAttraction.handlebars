<form id="selection" action="/attraction/editAttraction" method="POST">
        {{#if attractions.length}}
            <select name="attractionName" id="attractionName">
            {{#each attractions}}
                <option value="{{this.attractionName}}">{{this.attractionName}}</option>
            {{/each}}
            </select>
            <label for="submit">
                <button type="submit" name="submit" id="submit">Submit</button>
            </label>
        {{else}}
            <a href="/attractions">create an attraction first!</a>
        {{/if}}
</form>