
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>JavaScript File Upload</title>
</head>
<body>
    <!-- used cloudinary as a resource: https://cloudinary.com/blog/javascript_file_upload_to_cloudinary#setup_process -->
      <button id="upload">Upload files</button>

     <script src="https://widget.cloudinary.com/v2.0/global/all.js" type="text/javascript"></script>
     <script type="text/javascript">  
        var widget = cloudinary.applyUploadWidget({ cloudName: 'demo', uploadPreset: 'blog_upload' }, (error, result) { 
            if (!error && result && result.event === "success") { 
                    console.log('Done uploading..: ', result.info); 
                }

            });    

        //this code doesn't work yet :/
        document.getElementById("upload").addEventListener("click", upload);
        function upload(){
            console.log("is connected")
            //widget.open();
        }
     </script>
</body>
</html>

